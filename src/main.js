function chat(event) {
    event.setCancelled(true);
    chatCustom(event);
}

function damaged(event){
    damagedCustom(event);
}

function timer(event){
    timerCustom(event);
}

function attack(event) {
    attackCustom(event)
}